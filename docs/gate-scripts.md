# Программируемые gate-сигналы

Gate-блок в режиме `Script` хранит исходный текст в проекте и вычисляет
детерминированную функцию времени. Это ограниченный C-подобный язык выражений,
а не запуск произвольного нативного C/C++ кода.

```c
const double base = 500;
double frequency = base * 2;
double duty = 1.0 / 4.0;
return pwm(frequency, duty, 100e-6);
```

Поддерживаются объявления `double`, `bool`, `auto` и `const`-варианты,
арифметика `+ - * /`, скобки, сравнения, `==`, `!=`, `&&`, `||`, `!`, значения
`true`/`false` и `return`. Имена регистронезависимы. Переменные могут ссылаться
друг на друга; циклическая или неизвестная ссылка выдаёт
`invalid_gate_script` с UUID gate-блока.

Функции:

- `pwm(frequency, duty, delay)` (`square` — синоним): частота в Hz, заполнение
  от 0 до 1, задержка в секундах;
- `phasepwm(frequency, duty, delay)`: периодический сигнал с фазовой задержкой;
- `ramp(t0, t1, value0, value1)`: линейный переход между двумя значениями;
- `abs`, `min`, `max`, `sqrt`, `pow`.

Единственная переменная среды сейчас — `t`, текущее модельное время в секундах.
Значения узлов, токов и параметров схемы намеренно не доступны: поэтому скрипт
не образует неявную алгебраическую обратную связь с решателем.

Если верхнее выражение — PWM с постоянными частотой, заполнением и задержкой,
компилятор сразу создаёт точные фронты. Для зависящего от времени выражения
функция проверяется через `script_step`; смена состояния становится записанным
gate-событием в момент проверки. Таким образом, точность произвольной функции
явно задаётся пользователем, а обычный PWM не округляется к этому шагу.

Скрипт не имеет ввода-вывода, доступа к ОС, динамической памяти, циклов и
изменяемого состояния между вызовами. При одинаковом проекте и версии ядра
последовательность событий воспроизводима. Snapshot хранит уже скомпилированное
состояние расчёта; изменение исходного скрипта относится к новому запуску.

Отдельный [код инициализации и выражения параметров](parameter-expressions.md)
используют тот же детерминированный парсер и адресную диагностику. Переменные
параметров пока не экспортируются в `gate_script`; API задержанных результатов
расчёта остаётся следующим отдельным слоем.
